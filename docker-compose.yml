version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: ipims_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-ipims}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ipims_password}
      POSTGRES_DB: ${POSTGRES_DB:-ipims_db}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ipims} -d ${POSTGRES_DB:-ipims_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  postgres_data:
    driver: local
